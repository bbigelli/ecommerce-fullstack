services:
  - type: web
    name: ecommerce-api
    runtime: python
    repo: https://github.com/bbigelli/ecommerce-api
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ecommerce-db
          property: connectionString
      - key: SECRET_KEY
        sync: false
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 30
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT

databases:
  - name: ecommerce-db
    databaseName: ecommerce
    user: ecommerce_user
